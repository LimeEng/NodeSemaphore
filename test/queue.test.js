'use strict'

const assert = require('assert').strict
const utility = require('../lib/queue.js')

describe('Queue', function () {
  describe('.offer(value)', function () {
  
  })
  describe('.poll()', function () {
  
  })
  describe('.peek()', function () {
  
  })
  describe('.clear()', function () {
  
  })
})
